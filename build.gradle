apply plugin: 'com.android.application'

android {
    signingConfigs {
        Sebasu {
            keyAlias 'sebasuk'
            keyPassword 'Sehnlich69K'
            storeFile file('L:\\5_WorkSpaces\\AndroidStudioProjects\\AndroidKeyStore.jks')
            storePassword 'Kagutsuchi2.4'
        }
    }
    compileSdkVersion 31
    defaultConfig {
        applicationId "com.vagoscorp.whatthebyte"
        minSdkVersion 3
        targetSdkVersion 31
        versionCode 20200628
        versionName '1.0.5'
        signingConfig signingConfigs.Sebasu
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.Sebasu
        }
        debug {
            signingConfig signingConfigs.Sebasu
        }
    }
    productFlavors {
    }
}

dependencies {
}

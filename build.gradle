apply plugin: 'com.android.application'

android {
    signingConfigs {
        Sebasu {
            keyAlias 'sebasuk'
            keyPassword 'Sehnlich69K'
            storeFile file('L:\\AndroidStudioProjects\\AndroidKeyStore.jks')
            storePassword 'Kagutsuchi2.4'
        }
    }
    compileSdkVersion 30
    defaultConfig {
        applicationId "com.vagoscorp.whatthebyte"
        minSdkVersion 3
        targetSdkVersion 30
        versionCode 20200502
        versionName '1.0.4'
        signingConfig signingConfigs.Sebasu
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.Sebasu
        }
        debug {
            signingConfig signingConfigs.Sebasu
        }
    }
    productFlavors {
    }
}

dependencies {
}

apply plugin: 'com.android.application'

android {
    signingConfigs {
        Sebasu {
            keyAlias 'sebasuk'
            keyPassword 'Sehnlich69K'
            storeFile file('../../AndroidKeyStore.jks')
            storePassword 'Kagutsuchi2.4'
        }
    }
    defaultConfig {
        applicationId "com.vagoscorp.whatthebyte"
        minSdkVersion 3
        targetSdk 37
        versionCode 20260916
        versionName '1.0.5'
        signingConfig signingConfigs.Sebasu
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.Sebasu
        }
        debug {
            signingConfig signingConfigs.Sebasu
        }
    }
    productFlavors {
    }
    compileSdk 37
    namespace 'com.vagoscorp.whatthebyte'
    compileSdkMinor 2
}

dependencies {
}
